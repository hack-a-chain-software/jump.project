[package]
name = "staking"
version = "0.1.0"
authors = ["Near Inc <hello@near.org>"]
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-sdk = "3.1.0"
serde = "1"
serde_json = "1"

[dev-dependencies]
near-sdk-sim = "3.1.0"
