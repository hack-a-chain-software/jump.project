{
  "name": "near.monorepo",
  "version": "1.0.0",
  "description": "Near Contract and React Monorepo",
  "private": true,
  "scripts": {
    "start": "yarn workspace @near/web dev",
    "prestart": "yarn workspace @near/contracts prestart",
    "web:dev": "yarn workspace @near/web dev",
    "web:build": "yarn workspace @near/web build",
    "contract:build": "yarn workspace @near/contracts build:contract",
    "contract:cargo:tests": "yarn workspace @near/contracts test:rust",
    "contract:lib:tests": "echo 'Not implmented yet coming soon'",
    "postinstall": "husky install",
    "format:rust": "./format_rust.sh",
    "precommit": "lint-staged"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn precommit"
    }
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "yarn prettier --config .prettierrc --write **/*.{ts,tsx}",
      "git add ."
    ],
    "*.{js,jsx}": [
      "yarn prettier --config .prettierrc --write **/*.{js,jsx}",
      "git add ."
    ],
    "*.rs": [
      "sh ./format_rust.sh",
      "git add ."
    ]
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/tailwindcss",
      "**/tailwindcss/**",
      "**/postcss",
      "**/postcss/**",
      "**/autoprefixer",
      "**/autoprefixer/**"
    ]
  },
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "^16.0.1",
    "@commitlint/config-conventional": "^16.0.0",
    "@types/jest": "^27.5.0",
    "@typescript-eslint/eslint-plugin": "^5.22.0",
    "@typescript-eslint/parser": "^5.22.0",
    "eslint": "^8.15.0",
    "eslint-plugin-react": "^7.29.4",
    "husky": "^8.0.1",
    "lint-staged": "^12.4.1",
    "nodemon": "^2.0.15",
    "prettier": "^2.6.2"
  }
}
